
name: 'akahu_mortgage'
version: '1.0.0'
config-version: 2

profile: 'akahu_mortgage'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  akahu_mortgage:
    akahu:
      +materialized: view
